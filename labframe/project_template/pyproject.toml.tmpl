[project]
name = "{{PROJECT_NAME}}"
version = "0.1.0"
description = "A configuration-driven Labframe simulation or experiment project"
requires-python = ">=3.11"
dependencies = [
    "labframe",
    "matplotlib>=3.10",
    "numpy>=2.2",
    "pyyaml>=6.0",
    "scipy>=1.15",
]
{{UV_SOURCE_BLOCK}}
